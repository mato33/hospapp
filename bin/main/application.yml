server:
  port: 8000
  servlet:
    encoding:
      charset: utf-8
      
spring:
  datasource:
    url: jdbc:oracle:thin:@easy0g.iptime.org:1521:xe
    driver-class-name: oracle.jdbc.OracleDriver
    username: mess
    password : mess

  jpa:
    database-platform: org.hibernate.dialect.Oracle12cDialect
    open-in-view: true
    hibernate:
      ddl-auto: create
      #naming:
      #  physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true